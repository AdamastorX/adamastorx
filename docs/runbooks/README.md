# Runbooks

Placeholder. Operational runbooks (what to do when an alert fires, how to
roll back a bad ArgoCD sync, how to rotate a secret) live in the
[observability](https://github.com/AdamastorX/observability) repo under
`runbooks/`, next to the alerts they respond to — alert and runbook should
never drift apart in separate repos.

This folder stays reserved for org-level process runbooks only (e.g. "how to
cut a milestone", "how to onboard a new repo") if/when those are needed.
